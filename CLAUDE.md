@AGENTS.md

## Claude Code notes

- 标准项目上下文（环境、测试、lint、约定）在 `AGENTS.md`，上方 import 进来。
  改在那里，Cursor / Codex / 其他 agent 才能跟上。
- `AGENTS.md` 里打 `✅ verified <date>` 的命令是当时真跑过的 —— 优先信它们，不要猜。
- 唯一权威设计文档：`docs/designs/agents/shareholder-network/design.md`。
